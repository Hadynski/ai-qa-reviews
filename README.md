# Hadynski

Call recording QA system for Daktela.

## Quick Start

```bash
cp .env.example .env.local  # configure API keys
npx convex dev --local      # run backend
npm run dev                 # run frontend at localhost:3000
```
